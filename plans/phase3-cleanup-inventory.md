# Phase 3：冗余资源与死代码扫描报告（Local-Album）

> 基线：assembleFullDebug/LiteDebug + androidTest 编译 + 全部单测均绿色。
> 方法：全源码（main/full/lite/test/androidTest）逐类 regex 引用搜索，引用命中数为生产源集命中（test/androidTest 命中单独标注）。
> 原则：宁可漏报不可误报；所有"死代码"结论均附调用链证据与置信度。

---

## 1. 资源层清单（app/src/main/res，full/lite 无 res 目录）

递归确认：res 下仅有 values/strings.xml、values/themes.xml、values-night/themes.xml、xml/file_provider_paths.xml 与 5 个密度目录各 2 个 mipmap 图标，无遗漏。

| 资源                                                                                                                                                                                                                                                                                                                     | 类型   | 引用位置                                                                                                             | 结论               |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------ | -------------------------------------------------------------------------------------------------------------------- | ------------------ |
| `mipmap-*/ic_launcher.png`（5 文件）                                                                                                                                                                                                                                                                                     | 图标   | `AndroidManifest.xml:30` `android:icon`                                                                              | 保留               |
| `mipmap-*/ic_launcher_round.png`（5 文件）                                                                                                                                                                                                                                                                               | 图标   | `AndroidManifest.xml:32` `android:roundIcon`                                                                         | 保留               |
| `@style/Theme.LocalAlbum`（values/themes.xml + values-night/themes.xml）                                                                                                                                                                                                                                                 | 主题   | `AndroidManifest.xml:34` `android:theme`                                                                             | 保留               |
| `@xml/file_provider_paths`                                                                                                                                                                                                                                                                                               | XML    | `AndroidManifest.xml:42` provider meta-data                                                                          | 保留               |
| `string/app_name`                                                                                                                                                                                                                                                                                                        | 字符串 | `AndroidManifest.xml:31`                                                                                             | 保留               |
| `scan_notif_title` / `scan_notif_scanning` / `scan_notif_analyzing` / `scan_notif_stage`                                                                                                                                                                                                                                 | 字符串 | `ScanWorker.kt:68-69`、`ScanForegroundService.kt:39,88`、`AnalysisWorker.kt:154,306`、`AppContainer.kt:579`          | 保留               |
| `scan_channel_name` / `scan_channel_desc` / `enhancement_channel_name` / `enhancement_channel_desc` / `enhancement_notif_title`                                                                                                                                                                                          | 字符串 | `ScanServiceController.kt:113,178-197`                                                                               | 保留               |
| **其余 38 个 string**（nav*\* 6 个、action*_ 10 个、scan*scanning/scan_complete/scan_incremental/scan_full、trash*_ 4 个、search*\* 5 个、plugin_import/plugin_manage/plugin_enabled/plugin_disabled/plugin_loading/plugin_error/plugin_ready、face_unknown/face_clustering/face_swap、settings*_ 6 个、common\__ 5 个） | 字符串 | 全代码 `R.string.` 与 `stringResource(` 搜索 **0 命中**（full/lite 源集亦 0 命中）；UI 全部为 Compose 硬编码中文文案 | **可删**（高置信） |

补充验证建议：`gradlew lintFullDebug` 可作为最终确认（UnusedResources 检查），非必须。

---

## 2. 死代码清单

### 第一梯队：孤立高置信死代码

| #   | 文件路径                                                                                                           | 类名                                   | 行数 | 引用证据                                                                                                                                                                    | 传递依赖（独占引用） | 置信度 | 需同步修改的测试 |
| --- | ------------------------------------------------------------------------------------------------------------------ | -------------------------------------- | ---- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------- | ------ | ---------------- |
| 1   | `app/src/main/java/com/renyxin/localalbum/core/saf/SafeFileOperator.kt`                                            | `SafeFileOperator`                     | 437  | 全源码搜索仅命中自身定义（0 外部引用，含 test）                                                                                                                             | 无                   | 高     | 无               |
| 2   | `app/src/main/java/com/renyxin/localalbum/core/plugin/capability/CapabilityRegistry.kt`                            | `CapabilityRegistry`（V1）             | 301  | `CapabilityRegistry(` 构造搜索全源码 0 命中（含 test）；仅 V2 KDoc 与 `ExtensionPluginRegistry.kt:28` KDoc 提及"保留兼容"                                                   | 无                   | 高     | 无               |
| 3   | `app/src/main/java/com/renyxin/localalbum/core/plugin/capability/adapters/ClassificationToSceneProviderAdapter.kt` | `ClassificationToSceneProviderAdapter` | 39   | 构造搜索 0 命中，仅自身定义                                                                                                                                                 | 无                   | 高     | 无               |
| 4   | `app/src/main/java/com/renyxin/localalbum/core/plugin/capability/adapters/SceneProviderToClassificationAdapter.kt` | `SceneProviderToClassificationAdapter` | 89   | 构造搜索 0 命中，仅自身定义                                                                                                                                                 | 无                   | 高     | 无               |
| 5   | `app/src/main/java/com/renyxin/localalbum/core/plugin/model/BatchedModelLoader.kt`                                 | `BatchedModelLoader`                   | 127  | 仅自身 KDoc 示例引用                                                                                                                                                        | 无                   | 高     | 无               |
| 6   | `app/src/main/java/com/renyxin/localalbum/core/plugin/model/InferenceMemoryPool.kt`                                | `InferenceMemoryPool`                  | 153  | 仅自身 KDoc 示例引用                                                                                                                                                        | 无                   | 高     | 无               |
| 7   | `app/src/main/java/com/renyxin/localalbum/core/pipeline/PluginAnalysisStage.kt`                                    | `PluginAnalysisStage`                  | 134  | 构造搜索 0 命中；仅 `AnalysisStage.kt:29` KDoc 与 `PluginRegistry.kt:291` KDoc 引用；`PluginRegistry.getLoadedPlugins()`（其唯一 KDoc 声称的调用方）本身也是 0 调用的死方法 | 无                   | 高     | 无               |

### 第二梯队：Builtin\*Stage 旧版五件套 + 传递死代码簇

| #   | 文件路径                                                                                                   | 类名                           | 行数 | 引用证据                                                                                                                                                                                                                                                                                                                                                      | 传递依赖                                                                                       | 置信度                             | 需同步修改的测试                                                                                        |
| --- | ---------------------------------------------------------------------------------------------------------- | ------------------------------ | ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------------------------- |
| 8   | `app/src/full/java/com/renyxin/localalbum/core/pipeline/stages/BuiltinFaceStage.kt`                        | `BuiltinFaceStage`             | 145  | 生产构造 0 命中。注意：`FaceClusterMaintenanceWorker.kt:174` 引用其 `BUILTIN_PROVIDER_SCOPE`/`BUILTIN_MODEL_SCOPE` 两个**常量**（需先将常量迁至 Worker 或 companion 提取处）                                                                                                                                                                                  | 无独占依赖（`FaceDetector`/`FaceClusterer`/`IncrementalFaceClusterAssigner` 均有其他生产引用） | 高（删除前需迁移 2 个常量）        | `BoundedDataAccessArchitectureTest.kt:66,190,526`（读取该文件文本断言）                                 |
| 9   | `app/src/full/java/com/renyxin/localalbum/core/pipeline/stages/BuiltinOcrStage.kt`                         | `BuiltinOcrStage`              | 70   | 生产构造 0 命中，仅架构测试字符串引用                                                                                                                                                                                                                                                                                                                         | 无                                                                                             | 高                                 | `BoundedDataAccessArchitectureTest.kt:194,459`                                                          |
| 10  | `app/src/full/java/com/renyxin/localalbum/core/pipeline/stages/BuiltinSemanticStage.kt`                    | `BuiltinSemanticStage`         | 110  | 生产构造 0 命中，仅架构测试字符串引用                                                                                                                                                                                                                                                                                                                         | 无独占（`SemanticEmbedder` 被 `ConceptEmbedProvider` 生产引用）                                | 高                                 | `BoundedDataAccessArchitectureTest.kt:193,527`                                                          |
| 11  | `app/src/main/java/com/renyxin/localalbum/core/pipeline/stages/BuiltinSceneStage.kt`                       | `BuiltinSceneStage`            | 82   | 生产构造 0 命中（`AnalysisStageFactory` 已改用 `SceneStage` Provider 包装版，见 `AnalysisStageFactory.kt:58-60`）                                                                                                                                                                                                                                             | 无独占（`SceneClassifier` 被 `HeuristicSceneProvider` 生产引用）                               | 高                                 | `BoundedDataAccessArchitectureTest.kt:191`（该测试断言"Pipeline 不得引用 Builtin\*"，删除后断言仍成立） |
| 12  | `app/src/main/java/com/renyxin/localalbum/core/pipeline/stages/BuiltinQualityStage.kt`                     | `BuiltinQualityStage`          | 78   | 生产构造 0 命中（工厂已改用 `QualityStage`，见 `AnalysisStageFactory.kt:61-63`；文件内注释自证"policy-aware factory no longer uses it"）                                                                                                                                                                                                                      | 无独占（`QualityAnalyzer` 被 `HeuristicQualityProvider` 生产引用）                             | 高                                 | `BoundedDataAccessArchitectureTest.kt:192`                                                              |
| 13  | `app/src/main/java/com/renyxin/localalbum/core/plugin/model/MobileSAMPlugin.kt`                            | `MobileSAMPlugin`              | 242  | 构造搜索 0 命中。唯一"引用"是自身 manifest JSON 内的 entryClass 字符串（自指）；`PluginLoader` 的反射只作用于**外部 APK** 的 entryClass，主 APK 内字符串不构成生产链路                                                                                                                                                                                        | 独占引用 `ModelDownloadManager`（V1）→ 传递死代码 #14                                          | 高                                 | 无                                                                                                      |
| 14  | `app/src/main/java/com/renyxin/localalbum/core/plugin/model/ModelDownloadManager.kt`                       | `ModelDownloadManager`（V1）   | 156  | `ModelDownloadManager.ensureModel` 全源码唯一调用方是 `MobileSAMPlugin.kt:127`（自身死代码）；生产下载链路全部走 V2（`ModelManagerImpl.kt:239,295`、`PluginViewModel.kt:872,956`）                                                                                                                                                                            | 无                                                                                             | 高（依赖 #13 先删或同批删除）      | 无                                                                                                      |
| 15  | `app/src/main/java/com/renyxin/localalbum/core/plugin/ProgressManager.kt`（**部分删除**：类主体 1-240 行） | `ProgressManager`（plugin 包） | ~240 | 同包及全源码构造搜索 0 命中（pipeline 版 `core/pipeline/ProgressManager` 是生产版，被 `PluginAnalysisPipeline.kt:58`、`AlbumViewModel`、`GlobalProgressIndicator` 引用）。**注意**：同文件尾部 `TaskProgress` data class（258-272 行）被 `AlbumViewModel.kt:15,96-131` 生产引用，必须保留（建议将 `TaskProgress` 迁移至独立文件或并入 `AnalysisProgress.kt`） | 无                                                                                             | 高（但为部分删除，需重构文件拆分） | `ProgressManagerTest.kt` 测的是 pipeline 版，不受影响                                                   |
| 16  | `PluginRegistry.getLoadedPlugins()`（`PluginRegistry.kt:293`，**死方法而非死文件**）                       | —                              | 3    | 全源码 `getLoadedPlugins()` 调用 0 命中，仅自身定义                                                                                                                                                                                                                                                                                                           | 无                                                                                             | 高                                 | 无                                                                                                      |

### 仅测试使用（非死代码但需标注）

| 文件                                                            | 说明                                                                                                                                                                                  | 建议                                                                                                      |
| --------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| `core/index/ScanBenchmarkConfig.kt`（object 主体，约 26-80 行） | `scenarios`/`datasets`/`percentile` 仅被 `ScanBenchmarkConfigTest` 使用；**但同文件**的 `ScanMilestone` enum 与 `ScanTelemetry` 被 `HybridIndexer.kt:81-999` 生产引用，不可整文件删除 | 保留文件；object 主体属"仅测试使用"，中置信，若删需保留同文件 enum/telemetry——建议移交 Phase 4 做文件拆分 |
| `core/analysis/SemanticEmbedder.kt`                             | 生产链路为 `ConceptEmbedProvider`（AppContainer 注册）+ `BuiltinSemanticStage`（死）+ `SemanticClusterRecommender`（活）；`SemanticEmbedderTest` 直接测试它                           | 活代码，保留                                                                                              |

---

## 3. 重复封装对比表（V1/V2 影子类）

| 对比项                      | V1                                                                       | V2                                                  | 生产链路归属                                                                                                                    | 合并建议                                                           |
| --------------------------- | ------------------------------------------------------------------------ | --------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------ |
| 能力注册表                  | `CapabilityRegistry`（301 行，类型化字段注册表）                         | `CapabilityRegistryV2`（泛型 SlotEntry）            | **V2**：`AppContainer`、`EditionAnalysisStageBindings`、`DemoPluginsInitializer`、`AnalysisStageFactory` 全部使用 V2；V1 零构造 | 直接删除 V1（第一梯队 #2），V2 KDoc 中"保留兼容"注释一并清理       |
| 模型下载                    | `ModelDownloadManager`（object，静态 ensureModel，156 行）               | `ModelDownloadManagerV2`（class，可注入、带状态）   | **V2**：`MainActivity:73`、`PluginViewModel:60`、`ModelManagerImpl:239`                                                         | 随 MobileSAMPlugin 删除（第二梯队 #14）                            |
| ModelDownloadManagerV2 实例 | `MainActivity:73` 为 `PluginViewModel` 每次 new 一个                     | `ModelManagerImpl:239` 内部又持有一个               | 两处并存 → 同一 App 存在 2+ 个下载器实例，缓存/去重状态不共享                                                                   | **属重构非删除，移交 Phase 4**：由 `AppContainer` 统一构造单例注入 |
| 进度管理                    | `core/plugin/ProgressManager`（多任务+ETA，272 行）                      | `core/pipeline/ProgressManager`（管道阶段进度）     | **pipeline 版**：`PluginAnalysisPipeline`/`AlbumViewModel`/`GlobalProgressIndicator`/`AppContainer` 通知链                      | 删除 plugin 版类主体，保留 `TaskProgress`（见 #15）                |
| 场景/质量 Stage             | `BuiltinSceneStage`/`BuiltinQualityStage`（硬编码 analyzer）             | `SceneStage`/`QualityStage`（Provider 注入）        | **Provider 版**：`AnalysisStageFactory.kt:58-63`                                                                                | 删除 Builtin 版（#11、#12）                                        |
| Face/Semantic/Ocr Stage     | `BuiltinFaceStage`/`BuiltinSemanticStage`/`BuiltinOcrStage`（full 源集） | `FaceStage`/`SemanticStage`/`OcrStage`（full 源集） | **新版**：`EditionAnalysisStageBindings.kt:30-50` 显式绑定新版                                                                  | 删除 Builtin 版（#8-#10），先迁移 #8 的 2 个 scope 常量            |

---

## 4. 仓库卫生项

| 项目                                        | 状态                                                                                                                                                                                                                                                        | 建议                                                                                                                |
| ------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------- |
| `opencv/java/build.gradle.orig.bak`         | 文件存在于工作区。`.gitignore` 的 `build/` 规则只忽略 `opencv/java/build/` 目录，**不覆盖** 该 .bak 文件；无任何 `*.bak` 忽略规则 → 极可能被 git 跟踪。**本环境无法执行 git 命令，删除前请用 `git ls-files -- opencv/java/build.gradle.orig.bak` 最终确认** | 若被跟踪：`git rm` 删除（内容为上游 build.gradle 原始备份，上游绑定源码不在清理范围，但备份文件无存在必要）。中置信 |
| `app/release.jks`                           | 出现在 VSCode 打开标签中，`.gitignore:45` 已有 `*.jks` 规则                                                                                                                                                                                                 | 大概率未被跟踪；若已被跟踪需立即 `git rm --cached` 并轮换密钥（安全风险项）                                         |
| `opencv/java/build/`（含 R.jar 等中间产物） | `.gitignore:37` 已忽略                                                                                                                                                                                                                                      | 无需处理                                                                                                            |

---

## 5. 分批删除建议

### 第一批：孤立高置信死代码（无任何牵连）

- 删除 #1-#7：`SafeFileOperator.kt`、`CapabilityRegistry.kt`(V1)、两个 adapters、`BatchedModelLoader.kt`、`InferenceMemoryPool.kt`、`PluginAnalysisStage.kt`
- 同步：清理 `CapabilityRegistryV2.kt:15,51` 的 V1 KDoc 引用、`ExtensionPluginRegistry.kt:28` KDoc；删除 `PluginRegistry.getLoadedPlugins()` 死方法（#16）
- 预计删除：**7 文件 + 1 方法，约 1280 行**
- 验证：`gradlew assembleFullDebug assembleLiteDebug test`

### 第二批：Builtin\*Stage 五件套 + 传递死代码簇

- 删除 #8-#14：5 个 Builtin\*Stage、`MobileSAMPlugin.kt`、`ModelDownloadManager.kt`(V1)
- 前置：将 `BuiltinFaceStage.BUILTIN_PROVIDER_SCOPE`/`BUILTIN_MODEL_SCOPE` 迁移至 `FaceClusterMaintenanceWorker`（唯一消费方）
- 同步修改测试：`BoundedDataAccessArchitectureTest.kt`（3 处读取 Builtin 文件文本的断言块需删除或改写为对 `FaceStage` 等新版的断言；第 189-197 行"Pipeline 不得引用 Builtin\*"的防回归断言**建议保留**）
- 预计删除：**7 文件，约 888 行**
- 验证：全量编译 + 全部单测 + androidTest 编译

### 第三批：部分删除/结构清理（含轻量重构，边界情况）

- #15：`core/plugin/ProgressManager.kt` 拆分——删除类主体（~240 行），`TaskProgress` 迁至新文件（如 `core/plugin/TaskProgress.kt`），更新 `AlbumViewModel` import
- strings.xml 清理：删除 38 个未引用 string（保留 app_name 与 9 个通知渠道 string）
- `ScanBenchmarkConfig.kt` object 主体的处置（仅测试使用部分）——建议移交 Phase 4 与文件拆分一并处理

### 移交 Phase 4（重构，不属于删除）

- `ModelDownloadManagerV2` 双实例收敛为 `AppContainer` 单例注入
- `ScanBenchmarkConfig.kt` 文件拆分（enum/telemetry 保留，benchmark 配置移至 test 源集或删除）

---

## 6. 汇总统计

| 指标                         | 数值                                                                                                                                                                                                                                                        |
| ---------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 可删 Kotlin 文件数（整文件） | **13**                                                                                                                                                                                                                                                      |
| 部分删除文件                 | 1（`plugin/ProgressManager.kt` 类主体）+ strings.xml                                                                                                                                                                                                        |
| 预计删除总行数               | **约 2,400 行**（第一/二批 ~2,168 + 第三批类主体 ~240）                                                                                                                                                                                                     |
| 可删 string 资源             | 38 个                                                                                                                                                                                                                                                       |
| 高置信项数量                 | 14（#1-#14）                                                                                                                                                                                                                                                |
| 中置信项                     | 2（.bak 跟踪状态待 git 确认、ScanBenchmarkConfig object 主体）                                                                                                                                                                                              |
| 风险点                       | ① #8 需先迁移 2 个 scope 常量；② `BoundedDataAccessArchitectureTest` 是基于源码文本的架构守卫测试，删 Builtin 文件会使其失败，需同步改写；③ #15 不能整文件删除（`TaskProgress` 是活代码）；④ lite flavor 不含 full 源集，第二批删除后必须双 flavor 编译验证 |
