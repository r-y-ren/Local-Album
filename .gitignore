# ── Android ──
*.apk
*.aab
*.dex
*.class
*.ap_
*.aab_
# ── plan ──
plans/
plan/
# ── Gradle ──
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
local.properties
gradle-app.setting

# ── IDE ──
.idea/
*.iml
*.iws
*.ipr
.vscode/
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json

# ── OS ──
.DS_Store
Thumbs.db
.directory
*~

# ── Build outputs ──
app/build/
app/.cxx/
app/release/
opencv/java/build/
*.hprof

# ── Generated ──
app/src/main/java/com/renyxin/localalbum/data/db/AppDatabase_Impl*
*.generated.*

# ── Secrets & signing ──
*.jks
*.keystore
keystore.properties
signing.properties
google-services.json

# ── Test artifacts ──
app/src/test/results/
app/src/androidTest/results/

# ── Personal / temporary files ──
swap face_0.json

# ── Duplicate / third-party directories (not project source) ──
ComfyUI-ReActor-main/
OpenCV-android-sdk/
new_clip/

# ── Large model binaries (NOT tracked — download via scripts/download_models.sh) ──
# Small config files (vocab.json, merges.txt, tokenizer_config.json, inference.yml)
# are kept in the repo — only large model binaries are excluded.
app/src/main/assets/models/*.onnx
app/src/main/assets/models/*.bin
app/src/main/assets/models/*.zip
app/src/main/assets/models/*.tflite
app/src/main/assets/models/*.ptl
app/src/main/assets/models/eva02_clip/*.onnx
app/src/main/assets/models/PP-OCRv5_mobile_rec_infer/*.onnx
app/src/main/assets/models/PP-OCRv6_small_det_infer/*.onnx
# 例外：MobileNetV3 场景分类模型随包打包（~21MB），需纳入版本控制
!app/src/main/assets/models/MobileNet-v3-Large.tflite
